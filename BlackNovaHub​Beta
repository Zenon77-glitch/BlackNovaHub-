--// 1. Load the Library
local cascade = loadstring(game:HttpGet("https://raw.githubusercontent.com/earkgemini/testlol/refs/heads/main/UI.luau"))()

--// Services
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

--// Global Variables
_G.HeadSize = 24
_G.HitboxEnabled = false
_G.ESPName = false
_G.ESPHealth = false
local minimizeKeybind = Enum.KeyCode.RightControl

--// [Functions] Hitbox Logic
local function isFriend(player)
    local success, result = pcall(function()
        return LocalPlayer:IsFriendsWith(player.UserId)
    end)
    return success and result
end

local function applyHitbox(plr)
    if _G.HitboxEnabled and plr ~= LocalPlayer and not isFriend(plr) then
        pcall(function()
            if plr.Character and plr.Character:FindFirstChild("Head") then
                local head = plr.Character.Head
                head.Size = Vector3.new(_G.HeadSize, _G.HeadSize, _G.HeadSize)
                head.CanCollide = false
                head.Transparency = 0.5
                head.Color = Color3.fromRGB(255, 0, 0)
                head.Massless = true
            end
        end)
    else
        pcall(function()
            if plr.Character and plr.Character:FindFirstChild("Head") then
                plr.Character.Head.Size = Vector3.new(2, 1, 1)
                plr.Character.Head.Transparency = 0
            end
        end)
    end
end

--// [Functions] ESP Name & Health Logic
local function createESP(plr)
    local function update()
        local connection
        connection = RunService.RenderStepped:Connect(function()
            if plr.Character and plr.Character:FindFirstChild("Head") and plr ~= LocalPlayer and plr.Character:FindFirstChild("Humanoid") then
                local head = plr.Character.Head
                local hum = plr.Character.Humanoid
                
                -- ตรวจสอบว่ามี BillboardGui เดิมอยู่หรือยัง
                local bgui = head:FindFirstChild("NovaESP")
                if (_G.ESPName or _G.ESPHealth) and plr.Character.Humanoid.Health > 0 then
                    if not bgui then
                        bgui = Instance.new("BillboardGui")
                        bgui.Name = "NovaESP"
                        bgui.Adornee = head
                        bgui.Size = UDim2.new(0, 100, 0, 50)
                        bgui.StudsOffset = Vector3.new(0, 3, 0)
                        bgui.AlwaysOnTop = true
                        bgui.Parent = head
                        
                        local textLabel = Instance.new("TextLabel")
                        textLabel.Name = "Info"
                        textLabel.BackgroundTransparency = 1
                        textLabel.Size = UDim2.new(1, 0, 1, 0)
                        textLabel.Font = Enum.Font.GothamBold
                        textLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
                        textLabel.TextStrokeTransparency = 0
                        textLabel.TextSize = 14
                        textLabel.Parent = bgui
                    end
                    
                    local nameText = _G.ESPName and plr.Name or ""
                    local healthText = _G.ESPHealth and (" [" .. math.floor(hum.Health) .. "/" .. math.floor(hum.MaxHealth) .. "]") or ""
                    bgui.Info.Text = nameText .. healthText
                    
                    -- เปลี่ยนสีตามเลือด
                    if _G.ESPHealth then
                        local hpPercent = hum.Health / hum.MaxHealth
                        bgui.Info.TextColor3 = Color3.fromHSV(hpPercent * 0.3, 1, 1) -- สีแดงไปเขียว
                    else
                        bgui.Info.TextColor3 = Color3.fromRGB(255, 255, 255)
                    end
                else
                    if bgui then bgui:Destroy() end
                end
            elseif not plr.Character or not plr.Character:FindFirstChild("Head") then
                 -- Clean up if character is gone
            end
        end)
    end
    coroutine.wrap(update)()
end

-- Setup ESP for all current and new players
for _, player in pairs(Players:GetPlayers()) do
    createESP(player)
end
Players.PlayerAdded:Connect(createESP)

RunService.RenderStepped:Connect(function()
    for _, plr in pairs(Players:GetPlayers()) do
        applyHitbox(plr)
    end
end)

local function titledRow(parent, title, subtitle)
	local row = parent:Row({ SearchIndex = title })
	row:Left():TitleStack({ Title = title, Subtitle = subtitle })
	return row
end

--// [UI Setup]
local app = cascade.New({
	WindowPill = true,
	Theme = cascade.Themes.Dark,
})

local window = app:Window({
	Title = "BlackNovaHub",
	Subtitle = "Mobile Optimized",
	Size = UDim2.fromOffset(480, 320),
})

--// [Mobile Toggle Button]
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "NovaToggle"
ScreenGui.Parent = (game:GetService("CoreGui") or LocalPlayer:WaitForChild("PlayerGui"))
ScreenGui.ResetOnSpawn = false

local ToggleBtn = Instance.new("TextButton")
ToggleBtn.Name = "ToggleButton"
ToggleBtn.Parent = ScreenGui
ToggleBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
ToggleBtn.BorderSizePixel = 0
ToggleBtn.Position = UDim2.new(0, 50, 0.5, 0) 
ToggleBtn.Size = UDim2.new(0, 50, 0, 50)
ToggleBtn.Text = ""

local ShieldIcon = Instance.new("ImageLabel")
ShieldIcon.Parent = ToggleBtn
ShieldIcon.BackgroundTransparency = 1
ShieldIcon.Size = UDim2.new(0.7, 0, 0.7, 0)
ShieldIcon.Position = UDim2.new(0.15, 0, 0.15, 0)
ShieldIcon.Image = cascade.Symbols.shield
ShieldIcon.ImageColor3 = Color3.fromRGB(255, 255, 255)

local Corner = Instance.new("UICorner")
Corner.CornerRadius = UDim.new(0, 12) 
Corner.Parent = ToggleBtn

local Stroke = Instance.new("UIStroke")
Stroke.Color = Color3.fromRGB(80, 80, 80)
Stroke.Thickness = 2
Stroke.Parent = ToggleBtn

-- ระบบลากปุ่ม
local dragging, dragInput, dragStart, startPos
ToggleBtn.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = ToggleBtn.Position
    end
end)
ToggleBtn.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)
UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        local delta = input.Position - dragStart
        ToggleBtn.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)
UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = false
    end
end)

ToggleBtn.MouseButton1Click:Connect(function()
    window.Minimized = not window.Minimized
end)

--// [Main Content]
local mainSection = window:Section({ Title = "Combat & Visuals", Disclosure = true })
local mainTab = mainSection:Tab({ Title = "Main", Icon = cascade.Symbols.shield, Selected = true })
local form = mainTab:Form()

do -- Bypass
    local row = titledRow(form, "Bypass Hitbox", "Execute Anti-Cheat Bypass")
    row:Right():Button({
        Label = "Execute",
        State = "Primary",
        Pushed = function()
            loadstring(game:HttpGet("https://raw.githubusercontent.com/Pixeluted/adoniscries/main/Source.lua",true))()
        end
    })
end

do -- ESP Name
    local row = titledRow(form, "ESP Name", "Show player names")
    row:Right():Toggle({
        Value = _G.ESPName,
        ValueChanged = function(self, state)
            _G.ESPName = state
        end
    })
end

do -- ESP Health
    local row = titledRow(form, "ESP Health", "Show player HP")
    row:Right():Toggle({
        Value = _G.ESPHealth,
        ValueChanged = function(self, state)
            _G.ESPHealth = state
        end
    })
end

do -- ESP Item
    local row = titledRow(form, "ESP Item", "Show items in world")
    row:Right():Toggle({
        Value = false,
        ValueChanged = function(self, state)
            if state then
                loadstring(game:HttpGet("https://pastefy.app/cgXmCW23/raw"))()
            else
                for _, v in pairs(game:GetService("Workspace"):GetDescendants()) do
                    if v:IsA("Highlight") or v:IsA("BillboardGui") and v.Name ~= "NovaESP" then v:Destroy() end
                end
            end
        end
    })
end

do -- Hitbox Toggle
    local row = titledRow(form, "Hitbox Master", "Enable big head")
    row:Right():Toggle({
        Value = _G.HitboxEnabled,
        ValueChanged = function(self, state)
            _G.HitboxEnabled = state
        end
    })
end

do -- Hitbox Size
    local row = titledRow(form, "Hitbox Size", "Adjust size (Default 24)")
    row:Right():Stepper({
        Value = _G.HeadSize,
        Fielded = true,
        ValueChanged = function(self, value)
            _G.HeadSize = value
        end
    })
end

--// [Settings]
local settingsSection = window:Section({ Title = "Settings", Disclosure = true })
local settingsTab = settingsSection:Tab({ Title = "Config", Icon = cascade.Symbols.gear })
local sForm = settingsTab:Form()

do
    local row = titledRow(sForm, "Minimize Key", "Toggle visibility (PC)")
    row:Right():KeybindField({
        Value = minimizeKeybind,
        ValueChanged = function(self, value)
            minimizeKeybind = value
        end
    })
end

UserInputService.InputEnded:Connect(function(input, gameProcessedEvent)
	if input.KeyCode == minimizeKeybind and not gameProcessedEvent then
		window.Minimized = not window.Minimized
	end
end)
